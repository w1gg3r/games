{
  "name": "tic-tac-toe-server",
  "version": "1.0.0",
  "dependencies": {
    "express": "^5.1.0",
    "http": "^0.0.1-security",
    "nodemon": "^3.0.2",
    "socket.io": "^4.7.3"
  },
  "scripts": {
    "start": "node server.js",
    "build": "npm install && cd client && npm install && npm run build"
  }
}